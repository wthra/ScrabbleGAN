# ScrabbleGAN: Semi-Supervised Handwriting Generation

![ScrabbleGAN Header](https://upload.wikimedia.org/wikipedia/commons/thumb/d/d0/Google_Colaboratory_SVG_Logo.svg/1200px-Google_Colaboratory_SVG_Logo.svg.png) โปรเจกต์นี้เป็นการจำลองและพัฒนาโมเดล **ScrabbleGAN** โดยใช้ **TensorFlow 2** สำหรับสร้างรูปภาพลายมือ (Handwritten Text Generation) ที่มีความยาวของคำแตกต่างกันได้ (Arbitrary-Length) โมเดลนี้ใช้สถาปัตยกรรมแบบ Semi-Supervised GAN ที่ประกอบไปด้วย 3 ส่วนหลักทำงานร่วมกัน เพื่อให้ได้ภาพลายมือที่สมจริงและสามารถอ่านออกได้

## 📌 สารบัญ
- [ภาพรวมของระบบ (System Overview)](#-ภาพรวมของระบบ-system-overview)
- [สถาปัตยกรรมโมเดล (Model Architecture)](#-สถาปัตยกรรมโมเดล-model-architecture)
- [ความต้องการของระบบ (Requirements)](#-ความต้องการของระบบ-requirements)
- [การเตรียมข้อมูล (Data Preparation)](#-การเตรียมข้อมูล-data-preparation)
- [การใช้งานและการเทรน (Training & Usage)](#-การใช้งานและการเทรน-training--usage)
- [การทดลองและผลลัพธ์ (Experiments & Results)](#-การทดลองและผลลัพธ์-experiments--results)

---

## 📋 ภาพรวมของระบบ (System Overview)

ระบบถูกออกแบบมาให้สร้างภาพลายมือจากข้อความ input โดยใช้เทคนิค Adversarial Training ร่วมกับ Recognizer เพื่อคอยกำกับดูแลความถูกต้องของตัวอักษร

* **Generator ($G$):** สร้างภาพจาก Noise ($z$) และ Label ($y$)
* **Discriminator ($D$):** แยกแยะภาพจริง vs ภาพที่สร้างขึ้น
* **Recognizer ($R$):** อ่านข้อความจากภาพ เพื่อคำนวณ CTC Loss ช่วยให้ Generator สร้างภาพที่ "อ่านออก" เป็นคำที่ต้องการ

---

## 🏗 สถาปัตยกรรมโมเดล (Model Architecture)

รายละเอียดของแต่ละ Network ที่ใช้ในโปรเจกต์:

### 1. Generator (`make_generator`)
ใช้สถาปัตยกรรมแบบ **ResNet-based with Upsampling**
* **Input:** Random Noise Vector ($z$) + Text Label ($y$)
* **Spatial Embedding:** ใช้ `SpatialEmbedding` layer แปลง Text Label เป็น vector และคูณกับ Noise เพื่อฝังข้อมูลตัวอักษรลงไปในทุกส่วนของภาพ
* **ResNetBlockUp:** บล็อก Convolution ที่ทำการ Upsampling ขยายขนาดภาพขึ้นเรื่อยๆ
* **Non-Local Block:** ใช้กลไก **Self-Attention** เพื่อจับความสัมพันธ์ของพิกเซลในระยะไกล (Global dependencies) ทำให้ลายมือมีความต่อเนื่อง

### 2. Discriminator (`make_discriminator`)
ใช้สถาปัตยกรรมแบบ **ResNet-based Classifier**
* **Input:** รูปภาพ (จริงหรือปลอม)
* **ResNetBlockDown:** ลดขนาดภาพลง (Downsampling) เพื่อดึง Feature
* **Loss Function:** รองรับทั้ง `Hinge Loss` และ `Non-saturating Loss`

### 3. Recognizer (`make_recognizer`)
ใช้สถาปัตยกรรมแบบ **CRNN (Convolutional Recurrent Neural Network)**
* **Process:** Conv Layers $\rightarrow$ Map-to-Sequence $\rightarrow$ Dense
* **Loss Function:** ใช้ **CTC Loss (Connectionist Temporal Classification)** ซึ่งจำเป็นมากสำหรับการอ่านข้อความลายมือที่มีความยาวไม่แน่นอน

---

## 💻 ความต้องการของระบบ (Requirements)

ติดตั้ง dependencies ที่จำเป็นด้วยคำสั่ง:

```bash
pip install tensorflow numpy opencv-python matplotlib gin-config imageio tensorflow-docs

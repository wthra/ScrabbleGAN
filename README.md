# ScrabbleGAN: Handwriting Generation with GAN

โปรเจกต์นี้เป็นการจำลองโมเดล **ScrabbleGAN** โดยใช้ TensorFlow 2 สำหรับสร้างรูปภาพลายมือ (Handwritten Text Generation) ที่มีความยาวของคำแตกต่างกันได้ (Arbitrary-Length) โดยใช้สถาปัตยกรรมแบบ Semi-Supervised GAN ที่ประกอบไปด้วย Generator, Discriminator และ Recognizer

## 📋 ภาพรวมของระบบ (System Overview)

วิธีการของเราประกอบด้วย 3 ส่วนหลักที่ทำงานร่วมกันในรูปแบบ Adversarial Training:

### 1. Generator (ตัวสร้างภาพ)
ทำหน้าที่สร้างรูปภาพลายมือจาก Noise vector ($z$) และข้อความที่ต้องการ ($y$)
- **Architecture:** ใช้โครงสร้างแบบ ResNet-based Generator ที่มีการ Upsampling
- **Key Components:**
    - `SpatialEmbedding`: แปลงข้อความ (Text Label) ให้เป็น Vector เพื่อฝังลงไปในกระบวนการสร้างภาพ ทำให้ควบคุมข้อความที่จะสร้างได้
    - `ResNetBlockUp`: บล็อกสำหรับขยายขนาดภาพ (Upsample) ทีละชั้น
    - `NonLocalBlock`: ใช้กลไก Self-Attention เพื่อให้โมเดลเรียนรู้ความสัมพันธ์ของพิกเซลในระยะไกลได้ดีขึ้น (ช่วยให้ลายมือมีความต่อเนื่อง)

### 2. Discriminator (ตัวแยกแยะภาพ)
ทำหน้าที่ตรวจสอบว่ารูปภาพที่รับเข้ามาเป็น "ภาพลายมือจริง" หรือ "ภาพที่สร้างขึ้น"
- **Architecture:** ใช้ ResNet-based Classifier ที่ลดขนาดภาพลง (Downsampling)
- **Loss Function:** ใช้ Hinge Loss หรือ Non-saturating Loss ในการคำนวณความผิดพลาด

### 3. Recognizer (ตัวอ่านข้อความ)
ทำหน้าที่อ่านข้อความจากรูปภาพ เพื่อตรวจสอบว่าภาพที่ Generator สร้างออกมานั้น "อ่านออก" และ "ตรงกับข้อความที่สั่ง" หรือไม่
- **Architecture:** ใช้โครงสร้างแบบ CRNN (Convolutional Recurrent Neural Network)
- **Loss Function:** ใช้ **CTC Loss (Connectionist Temporal Classification)** ซึ่งเหมาะสำหรับการอ่านข้อความที่มีความยาวไม่คงที่

---

## 🛠️ การเตรียมข้อมูล (Data Preparation)

เราใช้วิธีการจัดกลุ่มข้อมูล (Bucketing) ตามความยาวของคำ เพื่อให้โมเดลสามารถรองรับคำที่มีความยาวหลากหลายได้

- **Dataset:** IAM Handwriting Database (หรือชุดข้อมูลลายมืออื่นๆ)
- **Preprocessing:**
    - มีการใช้ `cv2.morphologyEx` (Morphological Closing) เพื่อลดจุดรบกวนและทำให้เส้นลายมือมีความชัดเจนขึ้น
    - ข้อมูลจะถูกแปลงและจัดเก็บลงในโฟลเดอร์ตามความยาวของคำ (เช่น โฟลเดอร์ `3` สำหรับคำที่มี 3 ตัวอักษร) เพื่อให้ง่ายต่อการดึงมาเทรน

---

## 🚀 การเทรนโมเดล (Training Process)

กระบวนการเทรนจะเกิดขึ้นพร้อมกันทั้ง 3 ส่วน (End-to-End Training):

1. **Discriminator Step:** แยกแยะภาพจริงออกจากภาพปลอม
2. **Recognizer Step:** เรียนรู้ที่จะอ่านข้อความจากภาพจริง (เพื่อเป็นพื้นฐานความถูกต้อง)
3. **Generator Step:** พยายามสร้างภาพให้หลอก Discriminator ได้สำเร็จ **และ** ต้องทำให้ Recognizer อ่านข้อความได้ถูกต้องด้วย (Balanced Loss)

```python
# ตัวอย่างโค้ดส่วน Training Step
g_loss_balanced = g_loss + r_fake_logits  # Loss รวมของ Generator มาจากทั้งการหลอก Discriminator และความถูกต้องของตัวอักษร
